services:
  postgres:
    image: postgres:17

    environment:
      POSTGRES_DB: whatsflow
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin

    ports:
      - "5432:5432"

  redis:
    image: redis:8

    ports:
      - "6379:6379"
